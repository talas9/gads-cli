"""Analysis sub-package for the Talas gads CLI.

Read-only analytical helpers that build on top of :mod:`gads_lib.ads`
(GAQL queries) to surface actionable insights — wasted spend, gaps, etc.
None of these modules mutate the account.
"""
